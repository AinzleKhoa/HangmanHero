<Window x:Class="g1_hangmanhero.Views.HangmanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hangman Hero" Height="400" Width="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding CurrentWordState}" FontSize="24" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{Binding GameResultMessage}" FontSize="18" Foreground="Red" HorizontalAlignment="Center"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Lives: " FontSize="16"/>
            <TextBlock Text="{Binding RemainingLives}" FontSize="16"/>
            <TextBlock Text=" Score: " FontSize="16"/>
            <TextBlock Text="{Binding Score}" FontSize="16"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <TextBox Text="{Binding GuessedLetter, UpdateSourceTrigger=PropertyChanged}" Width="50" MaxLength="1"/>
            <Button Content="Guess" Command="{Binding GuessCommand}" Margin="10,0"/>
            <Button Content="New Game" Command="{Binding StartNewGameCommand}"/>
        </StackPanel>
    </Grid>
</Window>